spec:
  template:
    spec:
      containers:
        - name: profile-web 
          env:
            - name: PROFILE_API_ENDPOINT
              value: https://api.uat-news.co.uk/audio/v1/user-profile
